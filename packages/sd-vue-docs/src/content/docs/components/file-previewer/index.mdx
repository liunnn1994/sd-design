---
title: 文件预览 FilePreviewer
description: 在弹层或页面区域中预览图片、视频、音频和 PDF 文件。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import FilePreviewerBasicDemo from '../../../../components/generated/file-previewer/basic.vue';
import filePreviewerBasicSource from '../../../../components/generated/file-previewer/basic.vue?raw';
import FilePreviewerControlledDemo from '../../../../components/generated/file-previewer/controlled.vue';
import filePreviewerControlledSource from '../../../../components/generated/file-previewer/controlled.vue?raw';
import FilePreviewerCustomContentDemo from '../../../../components/generated/file-previewer/custom-content.vue';
import filePreviewerCustomContentSource from '../../../../components/generated/file-previewer/custom-content.vue?raw';
import FilePreviewerCustomTitleDemo from '../../../../components/generated/file-previewer/custom-title.vue';
import filePreviewerCustomTitleSource from '../../../../components/generated/file-previewer/custom-title.vue?raw';
import FilePreviewerInlineDemo from '../../../../components/generated/file-previewer/inline.vue';
import filePreviewerInlineSource from '../../../../components/generated/file-previewer/inline.vue?raw';
import FilePreviewerMediaDemo from '../../../../components/generated/file-previewer/media.vue';
import filePreviewerMediaSource from '../../../../components/generated/file-previewer/media.vue?raw';

### 基本用法

通过 `type` 明确指定文件预览器，默认使用图片预览。

<DemoBlock source={filePreviewerBasicSource} mainFile="src/file-previewer-basic.vue">

<FilePreviewerBasicDemo client:only="vue" />

</DemoBlock>

### 视频、音频和 PDF

视频和音频默认使用 Video.js HTML 的预设 Web Components 预览器，PDF 使用内嵌文档预览；默认都会在全屏弹层中展示。

<DemoBlock source={filePreviewerMediaSource} mainFile="src/file-previewer-media.vue">

<FilePreviewerMediaDemo client:only="vue" />

</DemoBlock>

### 内嵌展示

设置 `fullscreen="false"` 后，图片、视频、音频和 PDF 都会直接在当前位置展示，不再打开全屏弹层。

<DemoBlock source={filePreviewerInlineSource} mainFile="src/file-previewer-inline.vue">

<FilePreviewerInlineDemo client:only="vue" />

</DemoBlock>

### 自定义内容

使用 `content` 插槽可以完全接管预览区域，插槽参数会透出当前文件、类型、状态和关闭方法等上下文。

<DemoBlock source={filePreviewerCustomContentSource} mainFile="src/file-previewer-custom-content.vue">

<FilePreviewerCustomContentDemo client:only="vue" />

</DemoBlock>

### 受控用法

通过 `v-model:visible` 控制预览器打开和关闭。

<DemoBlock source={filePreviewerControlledSource} mainFile="src/file-previewer-controlled.vue">

<FilePreviewerControlledDemo client:only="vue" />

</DemoBlock>

### 自定义标题

使用 `title` 插槽自定义文件预览器标题区域。

<DemoBlock source={filePreviewerCustomTitleSource} mainFile="src/file-previewer-custom-title.vue">

<FilePreviewerCustomTitleDemo client:only="vue" />

</DemoBlock>

## API

### `<file-previewer>` Props

| 参数名 | 描述 | 类型 | 默认值 |
| --- | --- | --- | :-: |
| src | 文件地址 | `string` | `-` |
| type | 预览类型，由参数决定使用哪个预览器 | `'image' \| 'video' \| 'audio' \| 'pdf'` | `'image'` |
| visible **(v-model)** | 是否可见 | `boolean` | `-` |
| default-visible | 默认是否可见，非受控 | `boolean` | `false` |
| fullscreen | 是否使用全屏弹层展示 | `boolean` | `true` |
| title | 标题 | `string` | `-` |
| mask-closable | 点击遮罩是否关闭，仅全屏弹层下生效 | `boolean` | `true` |
| closable | 是否显示关闭按钮，仅全屏弹层下生效 | `boolean` | `true` |
| esc-to-close | 是否支持 ESC 键关闭，仅全屏弹层下生效 | `boolean` | `true` |
| popup-container | 设置弹出框挂载点，同 `teleport` 的 `to` | `HTMLElement \| string` | `-` |
| render-to-body | 是否渲染到 body，仅全屏弹层下生效 | `boolean` | `true` |
| image-props | 图片预览器参数，会透传给 Image 组件 | `Record<string, unknown>` | `-` |
| media-props | 视频和音频预览器参数。`skin` 可设置为 `'default'`、`'minimal'`、`'native'` 或 `false`，`playerProps` 透传给 `video-player`，`skinProps` 透传给 skin Web Component，其余字段透传给内部 `video` / `audio` 元素 | `Record<string, unknown>` | `-` |
| pdf-props | PDF 预览器参数，会透传给内部 `iframe` 元素 | `Record<string, unknown>` | `-` |

### `<file-previewer>` Events

| 事件名         | 描述         | 参数               |
| -------------- | ------------ | ------------------ |
| close          | 关闭事件     | -                  |
| visible-change | 可见状态变化 | visible: `boolean` |

### `<file-previewer>` Slots

| 插槽名 | 描述 | 参数 |
| --- | --- | --- |
| title | 自定义标题 | - |
| content | 自定义预览内容 | `{ src, type, title, visible, fullscreen, status, loading, loaded, error, close, onLoad, onError }` |
