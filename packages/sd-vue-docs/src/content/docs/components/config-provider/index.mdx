---
title: 全局配置 ConfigProvider
description: 在应用的最外层进行配置，一次设置，全局生效。一般用于设置国际化语言等功能。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import ConfigProviderBasicDemo from '../../../../components/generated/config-provider/basic.vue';
import configProviderBasicSource from '../../../../components/generated/config-provider/basic.vue?raw';
import ConfigProviderEmptyDemo from '../../../../components/generated/config-provider/empty.vue';
import configProviderEmptySource from '../../../../components/generated/config-provider/empty.vue?raw';
import ConfigProviderRtlDemo from '../../../../components/generated/config-provider/rtl.vue';
import configProviderRtlSource from '../../../../components/generated/config-provider/rtl.vue?raw';
import ConfigProviderAllowClearDemo from '../../../../components/generated/config-provider/allow-clear.vue';
import configProviderAllowClearSource from '../../../../components/generated/config-provider/allow-clear.vue?raw';
import ConfigProviderAllowSearchDemo from '../../../../components/generated/config-provider/allow-search.vue';
import configProviderAllowSearchSource from '../../../../components/generated/config-provider/allow-search.vue?raw';
import ConfigProviderVirtualListPropsDemo from '../../../../components/generated/config-provider/virtual-list-props.vue';
import configProviderVirtualListPropsSource from '../../../../components/generated/config-provider/virtual-list-props.vue?raw';
import ConfigProviderThemeModeDemo from '../../../../components/generated/config-provider/theme-mode.vue';
import configProviderThemeModeSource from '../../../../components/generated/config-provider/theme-mode.vue?raw';
import ConfigProviderModalDrawerDefaultsDemo from '../../../../components/generated/config-provider/modal-drawer-defaults.vue';
import configProviderModalDrawerDefaultsSource from '../../../../components/generated/config-provider/modal-drawer-defaults.vue?raw';
import ConfigProviderDatePickerDefaultsDemo from '../../../../components/generated/config-provider/date-picker-defaults.vue';
import configProviderDatePickerDefaultsSource from '../../../../components/generated/config-provider/date-picker-defaults.vue?raw';
import ConfigProviderPaginationDefaultsDemo from '../../../../components/generated/config-provider/pagination-defaults.vue';
import configProviderPaginationDefaultsSource from '../../../../components/generated/config-provider/pagination-defaults.vue?raw';
import ConfigProviderSpinPropsDemo from '../../../../components/generated/config-provider/spin-props.vue';
import configProviderSpinPropsSource from '../../../../components/generated/config-provider/spin-props.vue?raw';

### 基本用法

设置国际化语言的基本用法。

<DemoBlock source={configProviderBasicSource} mainFile="src/config-provider-basic.vue">

<ConfigProviderBasicDemo client:only="vue" />

</DemoBlock>

### 自定义空状态元素

通过 `empty` 插槽可以全局自定义空状态元素。

<DemoBlock source={configProviderEmptySource} mainFile="src/config-provider-empty.vue">

<ConfigProviderEmptyDemo client:only="vue" />

</DemoBlock>

### RTL 视图

设置组件为从右向左阅读的视图。

<DemoBlock source={configProviderRtlSource} mainFile="src/config-provider-rtl.vue">

<ConfigProviderRtlDemo client:only="vue" />

</DemoBlock>

### 默认开启 allowClear

设置 `allow-clear` 后，支持清空按钮的组件会默认继承这个配置；如果组件自己显式传入了 `allow-clear` 或 `allowClear`，则以组件 props 为准。

<DemoBlock source={configProviderAllowClearSource} mainFile="src/config-provider-allow-clear.vue">

<ConfigProviderAllowClearDemo client:only="vue" />

</DemoBlock>

### 默认开启 allowSearch

设置 `allow-search` 后，支持搜索的组件会默认继承这个配置；如果组件自己显式传入了 `allow-search` 或 `allowSearch`，则以组件 props 为准。

<DemoBlock source={configProviderAllowSearchSource} mainFile="src/config-provider-allow-search.vue">

<ConfigProviderAllowSearchDemo client:only="vue" />

</DemoBlock>

### 默认下拉虚拟滚动参数

设置 `virtual-list-props` 后，Select、AutoComplete、Cascader、TreeSelect 这类下拉组件会默认继承这组虚拟滚动参数；如果组件自己显式传入了 `virtual-list-props`，则以组件 props 为准。

<DemoBlock source={configProviderVirtualListPropsSource} mainFile="src/config-provider-virtual-list-props.vue">

<ConfigProviderVirtualListPropsDemo client:only="vue" />

</DemoBlock>

### 局部主题模式

`ConfigProvider` 现在可以作为局部 ThemeProvider 使用。设置 `theme-mode` 后，组件库会在当前子树上挂载真正的局部 `sd-theme` 容器，而不是只依赖 `body`。

<DemoBlock source={configProviderThemeModeSource} mainFile="src/config-provider-theme-mode.vue">

<ConfigProviderThemeModeDemo client:only="vue" />

</DemoBlock>

### Modal / Drawer 默认配置

设置 `modal` 与 `drawer` 后，Modal / Drawer 会默认继承对应配置；如果组件自己显式传入了同名 props（例如 `closable`、`ok-text`、`width`），则以组件 props 为准。

<DemoBlock
	source={configProviderModalDrawerDefaultsSource}
	mainFile="src/config-provider-modal-drawer-defaults.vue"
>

<ConfigProviderModalDrawerDefaultsDemo client:only="vue" />

</DemoBlock>

### DatePicker 默认配置

设置 `date-picker` 后，DatePicker / RangePicker 会默认继承对应配置；如果组件自己显式传入了同名 props（例如 `show-confirm-btn`、`abbreviation`、`shortcuts`），则以组件 props 为准。

<DemoBlock
	source={configProviderDatePickerDefaultsSource}
	mainFile="src/config-provider-date-picker-defaults.vue"
>

<ConfigProviderDatePickerDefaultsDemo client:only="vue" />

</DemoBlock>

### Pagination 默认配置

设置 `pagination` 后，Pagination 会默认继承对应配置；如果组件自己显式传入了同名 props，则以组件 props 为准。下面第二个分页组件通过局部 props 关闭了页容量选择器，并单独开启了跳转输入框。

<DemoBlock
	source={configProviderPaginationDefaultsSource}
	mainFile="src/config-provider-pagination-defaults.vue"
>

<ConfigProviderPaginationDefaultsDemo client:only="vue" />

</DemoBlock>

### Spin 与 Table 加载参数

`spin-props` 统一设置所有 Spin 的默认属性；`table-spin-props`、`list-spin-props` 等组件级配置只设置对应组件内部的 Spin，并覆盖同名的通用配置。组件局部传入的 `spin-props` 优先级最高。

<DemoBlock
	source={configProviderSpinPropsSource}
	mainFile="src/config-provider-spin-props.vue"
>

<ConfigProviderSpinPropsDemo client:only="vue" />

</DemoBlock>

## API

### `<config-provider>` Props

| 参数名 | 描述 | 类型 | 默认值 | 版本 |
| --- | --- | --- | :-: | :-- |
| prefix-cls | 组件类名前缀 | `string` | `'sd'` |  |
| locale | 配置语言包 | `SdLang` | `-` |  |
| size | 大小 | `Size` | `-` | 2.14.0 |
| allow-clear | 是否默认开启清除按钮 | `boolean` | `false` |  |
| allow-search | 是否默认开启搜索 | `boolean` | `false` |  |
| virtual-list-props | 下拉类组件默认虚拟滚动参数 | `VirtualListProps` | `-` |  |
| global | 是否全局生效 | `boolean` | `false` | 2.25.0 |
| update-at-scroll | 是否在容器滚动时更新弹出框的位置 | `boolean` | `false` | 2.25.0 |
| scroll-to-close | 是否在滚动时关闭弹出框 | `boolean` | `false` | 2.46.0 |
| exchange-time | 是否交换时间 | `boolean` | `true` | 2.48.0 |
| rtl | 视图的表现形式是从右开始向左结束 | `boolean` | `false` |  |
| date-picker | DatePicker 组件默认配置 | `ConfigProviderDatePicker` | `-` |  |
| modal | Modal 组件默认配置 | `ConfigProviderModal` | `-` |  |
| drawer | Drawer 组件默认配置 | `ConfigProviderDrawer` | `-` |  |
| pagination | Pagination 组件默认配置 | `ConfigProviderPagination` | `-` |  |
| color-picker | ColorPicker 组件默认配置 | `ConfigProviderColorPicker` | `-` |  |
| spin-props | Spin 组件默认属性 | `SpinProps` | `-` |  |
| basic-crud-table-spin-props | BasicCrudTable 内部 Spin 的默认属性 | `SpinProps` | `-` |  |
| card-spin-props | Card 内部 Spin 的默认属性 | `SpinProps` | `-` |  |
| cascader-spin-props | Cascader 内部 Spin 的默认属性 | `SpinProps` | `-` |  |
| list-spin-props | List 内部 Spin 的默认属性 | `SpinProps` | `-` |  |
| qr-code-spin-props | QrCode 内部 Spin 的默认属性 | `SpinProps` | `-` |  |
| select-spin-props | Select 内部 Spin 的默认属性 | `SpinProps` | `-` |  |
| table-spin-props | Table 内部 Spin 的默认属性，优先于 `spin-props` | `SpinProps` | `-` |  |
| timeline-spin-props | Timeline 内部 Spin 的默认属性 | `SpinProps` | `-` |  |
| toolbar-spin-props | Toolbar 内部 Spin 的默认属性 | `SpinProps` | `-` |  |
| tree-select-spin-props | TreeSelect 内部 Spin 的默认属性 | `SpinProps` | `-` |  |
| json-form | JsonForm 组件默认配置 | `JsonFormProviderConfig` | `-` |  |
| theme | 运行时主题对象 | `SdThemeConfig` | `-` |  |
| theme-mode | 当前 ConfigProvider 子树的主题模式 | `'light' \| 'dark'` | `-` |  |

### `<config-provider>` Slots

| 插槽名  |       描述       | 参数                | 版本   |
| ------- | :--------------: | ------------------- | :----- |
| loading | 自定义加载中元素 | -                   | 2.28.0 |
| empty   | 自定义空状态元素 | component: `string` | 2.28.0 |

## FAQ

### 全局配置

`global` 属性设置为 `true` 时，会将配置内容注入到 Vue AppContext 中，一般用于解决使用 Modal、Message 组件的函数式调用方法时，配置内容无法生效的问题。

`allow-clear` 只会给未显式传入 `allowClear` 或 `allow-clear` 的组件提供默认值，不会覆盖组件自己已经声明的 props。

`allow-search` 也只会给未显式传入 `allowSearch` 或 `allow-search` 的组件提供默认值；像 `filterable` 这类兼容别名如果已经显式传入，同样会优先于全局配置。

`virtual-list-props` 目前只对下拉类组件生效，例如 Select、AutoComplete、Cascader、TreeSelect；Table、List 等非下拉组件不会读取这个全局默认值。

`modal` 支持以下全局默认字段：`mask`、`maskClosable`、`maskStyle`、`alignCenter`、`escToClose`、`draggable`、`closable`、`titleAlign`、`titleEllipsisTooltip`、`top`、`footer`、`hideCancel`、`okText`、`cancelText`、`okButtonProps`、`cancelButtonProps`、`width`。

`date-picker` 支持以下全局默认字段：`shortcuts`、`shortcutsPosition`、`previewShortcut`、`showConfirmBtn`、`showNowBtn`、`dayStartOfWeek`、`abbreviation`。

`drawer` 支持以下全局默认字段：`placement`、`mask`、`maskClosable`、`escToClose`、`closable`、`header`、`footer`、`titleEllipsisTooltip`、`hideCancel`、`okText`、`cancelText`、`okButtonProps`、`cancelButtonProps`、`width`、`height`。

`pagination` 支持以下全局默认字段：`pageSizeOptions`、`defaultPageSize`、`showTotal`、`showMore`、`showJumper`、`showPageSize`、`autoAdjust`、`baseSize`、`bufferSize`、`pageSizeProps`。

`color-picker` 支持通过 `swatchColors` 配置 ColorPicker 的默认系统色板。组件显式传入 `swatch-colors` 时优先使用组件配置；两处均未配置时系统色板为空。

`json-form` 支持给 `JsonForm` 提供默认 `adapter`，以及注入业务自定义字段组件注册表 `components`。当多个页面共享同一批业务字段组件时，优先通过 `ConfigProvider` 统一配置。

这些字段遵循“组件显式传参优先”原则：只有当组件未显式声明对应 prop 时，才会回退到 ConfigProvider 的默认值。

### 局部主题模式

`theme-mode` 会让 `ConfigProvider` 在当前子树中创建一个局部 ThemeProvider。和直接操作 `body` 不同，这种方式适合在同一页面上并存多个不同主题的组件区域。

当只传 `theme`、不传 `theme-mode` 时，当前子树会继承上层主题模式，仅对传入的 token 做局部增量覆盖。

如果你只需要局部主题切换，不需要 locale、rtl、size 等其他配置，也可以直接使用 `ThemeProvider` 组件。

### 自定义空状态展示

可以在 `#empty` 中自定义组件库全局的空状态展示，如果在插槽中使用到了 `Empty` 组件，需要开启 `inConfigProvider` 属性。
