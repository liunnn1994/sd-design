---
title: 颜色选择器 ColorPicker
description: 用于输入、预览和调整颜色的颜色选择器。
---

import DemoBlock from '../../../../components/docs/DemoBlock.astro';
import ColorPickerBasicDemo from '../../../../components/generated/color-picker/basic.vue';
import colorPickerBasicSource from '../../../../components/generated/color-picker/basic.vue?raw';
import ColorPickerGradientDemo from '../../../../components/generated/color-picker/gradient.vue';
import colorPickerGradientSource from '../../../../components/generated/color-picker/gradient.vue?raw';
import ColorPickerColorsDemo from '../../../../components/generated/color-picker/colors.vue';
import colorPickerColorsSource from '../../../../components/generated/color-picker/colors.vue?raw';
import ColorPickerDisabledDemo from '../../../../components/generated/color-picker/disabled.vue';
import colorPickerDisabledSource from '../../../../components/generated/color-picker/disabled.vue?raw';
import ColorPickerFormatDemo from '../../../../components/generated/color-picker/format.vue';
import colorPickerFormatSource from '../../../../components/generated/color-picker/format.vue?raw';
import ColorPickerOnlyPanelDemo from '../../../../components/generated/color-picker/only-panel.vue';
import colorPickerOnlyPanelSource from '../../../../components/generated/color-picker/only-panel.vue?raw';
import ColorPickerSizeDemo from '../../../../components/generated/color-picker/size.vue';
import colorPickerSizeSource from '../../../../components/generated/color-picker/size.vue?raw';
import ColorPickerTriggerElementDemo from '../../../../components/generated/color-picker/trigger-element.vue';
import colorPickerTriggerElementSource from '../../../../components/generated/color-picker/trigger-element.vue?raw';
import ColorPickerTriggerDemo from '../../../../components/generated/color-picker/trigger.vue';
import colorPickerTriggerSource from '../../../../components/generated/color-picker/trigger.vue?raw';

### 基本使用

默认使用输入型触发器。可以直接通过输入框编辑颜色，也可以展开面板精调。

<DemoBlock source={colorPickerBasicSource} mainFile="src/color-picker-basic.vue">

<ColorPickerBasicDemo client:only="vue" />

</DemoBlock>

### 渐变与模式切换

`color-modes` 现在完整支持 `monochrome` 和 `linear-gradient`。当允许渐变时，面板会提供模式切换、渐变 stop 编辑、角度设置，以及 gradient-only 的内联面板。

<DemoBlock source={colorPickerGradientSource} mainFile="src/color-picker-gradient.vue">

<ColorPickerGradientDemo client:only="vue" />

</DemoBlock>

### 预设颜色和历史颜色

通过 `recent-colors` 和 `swatch-colors` 控制最近使用颜色与系统色板。最近使用颜色变化时会触发 `recent-colors-change`。

<DemoBlock source={colorPickerColorsSource} mainFile="src/color-picker-colors.vue">

<ColorPickerColorsDemo client:only="vue" />

</DemoBlock>

### 禁用

设置 `disabled` 禁用选择器。

<DemoBlock source={colorPickerDisabledSource} mainFile="src/color-picker-disabled.vue">

<ColorPickerDisabledDemo client:only="vue" />

</DemoBlock>

### 颜色格式

通过 `format` 切换输出格式。单色模式覆盖 `HEX`、`HEX8`、`RGB`、`RGBA`、`HSL`、`HSLA`、`HSV`、`HSVA`、`CMYK`、`CSS` 的字符串输出；渐变模式统一输出 `linear-gradient(...)` CSS 字符串。

<DemoBlock source={colorPickerFormatSource} mainFile="src/color-picker-format.vue">

<ColorPickerFormatDemo client:only="vue" />

</DemoBlock>

### 只使用面板

使用 `hide-trigger` 可以内联渲染颜色面板。

<DemoBlock source={colorPickerOnlyPanelSource} mainFile="src/color-picker-only-panel.vue">

<ColorPickerOnlyPanelDemo client:only="vue" />

</DemoBlock>

### 尺寸

颜色选择器支持 `small`、`medium`、`large` 与 `mini` 四种尺寸。

<DemoBlock source={colorPickerSizeSource} mainFile="src/color-picker-size.vue">

<ColorPickerSizeDemo client:only="vue" />

</DemoBlock>

### 自定义触发元素

自定义触发元素。

<DemoBlock source={colorPickerTriggerElementSource} mainFile="src/color-picker-trigger-element.vue">

<ColorPickerTriggerElementDemo client:only="vue" />

</DemoBlock>

### 触发器

可以通过 `trigger-props` 直接透传 [TriggerProps](../../../components/trigger/index.html) 配置，行为与组件库里的 Trigger 保持一致。

<DemoBlock source={colorPickerTriggerSource} mainFile="src/color-picker-trigger.vue">

<ColorPickerTriggerDemo client:only="vue" />

</DemoBlock>

## API

### `<color-picker>` Props

| 参数名 | 描述 | 类型 | 默认值 |
| --- | --- | --- | :-: |
| model-value **(v-model)** | 受控色值 | `string` | `-` |
| default-value | 非受控默认色值 | `string` | `''` |
| color-modes | 可用颜色模式 | `('monochrome' \| 'linear-gradient')[]` | `['monochrome']` |
| enable-multiple-gradient | 是否允许新增多个渐变 stop | `boolean` | `true` |
| format | 输出格式 | `'HEX' \| 'HEX8' \| 'RGB' \| 'RGBA' \| 'HSL' \| 'HSLA' \| 'HSV' \| 'HSVA' \| 'CMYK' \| 'CSS'` | `'RGB'` |
| size | 尺寸 | `'mini' \| 'small' \| 'medium' \| 'large'` | `'medium'` |
| clearable | 是否允许清空 | `boolean` | `false` |
| enable-alpha | 是否启用透明通道 | `boolean` | `false` |
| show-primary-color-preview | 是否展示主色预览块 | `boolean` | `true` |
| input-props | 透传到触发输入框的属性 | `Record<string, unknown>` | `-` |
| trigger-props | 透传 Trigger 组件的属性 | `Partial<TriggerProps>` | `-` |
| recent-colors | 最近使用颜色 | `string[] \| boolean \| null` | `[]` |
| default-recent-colors | 最近使用颜色的非受控初始值 | `string[] \| boolean \| null` | `[]` |
| swatch-colors | 系统色板 | `string[] \| null` | 内置色板 |
| disabled | 禁用 | `boolean` | `false` |
| hide-trigger | 仅渲染内联面板 | `boolean` | `false` |

### `<color-picker>` Events

| 事件名 | 描述 | 参数 |
| --- | --- | --- |
| change | 颜色值变化时触发 | value: `string`<br />context: `{ color, trigger }` |
| clear | 点击清空时触发 | `{ e: MouseEvent }` |
| popup-visible-change | 颜色面板展开和收起时触发 | visible: `boolean`<br />value: `string` |
| palette-bar-change | 色相条或透明度条变化时触发 | `{ color }` |
| recent-colors-change | 最近使用颜色变化时触发 | `string[]` |

### `<color-picker>` Slots

| 插槽名  | 描述             |
| ------- | ---------------- |
| default | 自定义触发器内容 |
