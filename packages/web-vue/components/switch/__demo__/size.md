```yaml
title:
  zh-CN: 开关尺寸
  en-US: Switch Size
```

## zh-CN

开关分为 `small`、`medium` 两种尺寸。

---

## en-US

The switch is divided into two sizes, `small` and `medium`.

---

```vue
<template>
  <sd-space size="large">
    <sd-switch />
    <sd-switch size="small" />
  </sd-space>
</template>
```
